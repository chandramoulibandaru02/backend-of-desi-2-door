import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => (
  <footer style={{ background: '#1a1a1a', color: '#ccc', padding: '40px 0 20px', marginTop: 60 }}>
    <div className="container">
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: 32, marginBottom: 32 }}>
        <div>
          <div style={{ fontFamily: 'var(--font-display)', fontSize: 22, fontWeight: 800, color: 'var(--green-light)', marginBottom: 8 }}>
            🌿 Desi2Door
          </div>
          <p style={{ fontSize: 14, lineHeight: 1.6, color: '#999' }}>
            Farm-fresh groceries delivered to your doorstep. We bring the best of nature straight to your kitchen.
          </p>
          <a href="https://wa.me/919849854853" target="_blank" rel="noreferrer"
            style={{ display: 'inline-flex', alignItems: 'center', gap: 6, background: '#25D366', color: 'white', padding: '8px 14px', borderRadius: 8, fontSize: 13, fontWeight: 600, marginTop: 12, textDecoration: 'none' }}>
            💬 WhatsApp Us
          </a>
        </div>
        <div>
          <h4 style={{ color: 'white', marginBottom: 12, fontFamily: 'var(--font-display)' }}>Quick Links</h4>
          {[['/', 'Home'], ['/shop', 'Shop'], ['/cart', 'Cart'], ['/orders', 'My Orders']].map(([to, label]) => (
            <Link key={to} to={to} style={{ display: 'block', color: '#999', fontSize: 14, marginBottom: 6, textDecoration: 'none' }}
              onMouseEnter={e => e.target.style.color = 'var(--green-light)'}
              onMouseLeave={e => e.target.style.color = '#999'}>
              {label}
            </Link>
          ))}
        </div>
        <div>
          <h4 style={{ color: 'white', marginBottom: 12, fontFamily: 'var(--font-display)' }}>Categories</h4>
          {['Meat & Fish', 'Vegetables', 'Dairy & Eggs', 'Organic Products', 'Dry Fruits', 'Millets'].map(cat => (
            <div key={cat} style={{ color: '#999', fontSize: 14, marginBottom: 6 }}>{cat}</div>
          ))}
        </div>
        <div>
          <h4 style={{ color: 'white', marginBottom: 12, fontFamily: 'var(--font-display)' }}>Contact</h4>
          <div style={{ fontSize: 14, color: '#999', lineHeight: 2 }}>
            <div>📞 +91 98498 54853</div>
            <div>📧 support@desi2door.com</div>
            <div>🕐 9 AM – 7 PM Daily</div>
            <div style={{ marginTop: 8, fontSize: 12 }}>💳 Cash on Delivery Only</div>
          </div>
        </div>
      </div>
      <div style={{ borderTop: '1px solid #333', paddingTop: 16, textAlign: 'center', fontSize: 13, color: '#555' }}>
        © 2024 Desi2Door. All rights reserved. Made with ❤️ for fresh food lovers.
      </div>
    </div>
  </footer>
);

export default Footer;
