import React, { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { productAPI } from '../services/api';
import ProductCard from '../components/ProductCard';

const CATEGORIES = [
  { id: 'meat-fish', label: 'Meat & Fish', emoji: '🥩', color: '#fff1f2', border: '#fda4af', desc: 'Mutton, Chicken & Fish' },
  { id: 'vegetables', label: 'Vegetables', emoji: '🥦', color: '#f0fdf4', border: '#86efac', desc: 'Farm-Fresh Seasonal Veggies' },
  { id: 'dairy-eggs', label: 'Dairy & Eggs', emoji: '🥛', color: '#eff6ff', border: '#93c5fd', desc: 'Pure Milk & Desi Eggs' },
  { id: 'oils-ghee', label: 'Organic Products', emoji: '🫙', color: '#fefce8', border: '#fde047', desc: 'Ghee, Oils & Honey' },
  { id: 'millets-groceries', label: 'Millets & Flours', emoji: '🌾', color: '#fff7ed', border: '#fdba74', desc: 'Healthy Grains & Flours' },
  { id: 'dry-fruits', label: 'Dry Fruits', emoji: '🥜', color: '#fdf4ff', border: '#d8b4fe', desc: 'Premium Nuts & Dry Fruits' },
  { id: 'masalas', label: 'Masalas', emoji: '🌶️', color: '#fff1f2', border: '#fca5a5', desc: 'Natural Spice Blends' },
  { id: 'chocolates', label: 'Chocolates', emoji: '🍫', color: '#fef3c7', border: '#fbbf24', desc: 'Premium Chocolates' },
];

const HomePage = () => {
  const [featured, setFeatured] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    productAPI.getAll({ featured: true, limit: 8 })
      .then(res => { setFeatured(res.data.products || []); setLoading(false); })
      .catch(() => setLoading(false));
  }, []);

  return (
    <div className="page-fade">
      {/* Hero Banner */}
      <section style={{ background: 'linear-gradient(135deg, #15803d 0%, #16a34a 55%, #ca6d08 100%)', padding: '52px 0 44px', position: 'relative', overflow: 'hidden' }}>
        <div style={{ position: 'absolute', top: -60, right: -60, width: 280, height: 280, borderRadius: '50%', background: 'rgba(255,255,255,0.06)' }} />
        <div style={{ position: 'absolute', bottom: -80, left: -40, width: 320, height: 320, borderRadius: '50%', background: 'rgba(255,255,255,0.04)' }} />
        <div className="container" style={{ position: 'relative', zIndex: 1 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 32, flexWrap: 'wrap' }}>
            <div style={{ flex: 1, minWidth: 260, color: 'white' }}>
              <div style={{ background: 'rgba(255,255,255,0.18)', backdropFilter: 'blur(8px)', display: 'inline-flex', alignItems: 'center', gap: 6, padding: '6px 16px', borderRadius: 30, fontSize: 14, fontWeight: 700, marginBottom: 16 }}>
                🌿 100% Natural & Pure
              </div>
              <h1 style={{ fontFamily: 'var(--font-display)', fontSize: 'clamp(28px, 5vw, 52px)', fontWeight: 800, lineHeight: 1.15, marginBottom: 16 }}>
                Fresh From Farm,<br />
                <span style={{ color: '#fde047' }}>Right at Your Doorstep</span> 🏠
              </h1>
              <p style={{ fontSize: 18, opacity: 0.92, marginBottom: 28, lineHeight: 1.7 }}>
                Order fresh mutton, chicken, fish, vegetables, dairy & organic products. Delivered same day to your home!
              </p>
              <div style={{ display: 'flex', gap: 14, flexWrap: 'wrap', marginBottom: 32 }}>
                <Link to="/shop" style={{ background: '#ea580c', color: 'white', padding: '16px 36px', borderRadius: 14, fontWeight: 800, fontSize: 19, fontFamily: 'var(--font-display)', textDecoration: 'none', display: 'inline-flex', alignItems: 'center', gap: 8, boxShadow: '0 4px 16px rgba(234,88,12,0.4)', transition: 'transform 0.15s' }}
                  onMouseEnter={e => e.currentTarget.style.transform = 'translateY(-2px)'}
                  onMouseLeave={e => e.currentTarget.style.transform = 'translateY(0)'}>
                  🛒 Order Now
                </Link>
                <Link to="/shop?category=meat-fish" style={{ background: 'rgba(255,255,255,0.15)', color: 'white', padding: '16px 26px', borderRadius: 14, fontWeight: 700, fontSize: 16, fontFamily: 'var(--font-display)', border: '2px solid rgba(255,255,255,0.3)', textDecoration: 'none' }}>
                  🥩 Fresh Meat
                </Link>
              </div>
              <div style={{ display: 'flex', gap: 28, flexWrap: 'wrap' }}>
                {[['🚚', 'Free Delivery', 'On ₹499+'], ['⏰', 'Same Day', 'Delivery'], ['✅', 'No Password', 'Just Phone No.'], ['💳', 'Cash on', 'Delivery']].map(([icon, l1, l2]) => (
                  <div key={l1} style={{ textAlign: 'center' }}>
                    <div style={{ fontSize: 26, marginBottom: 2 }}>{icon}</div>
                    <div style={{ fontSize: 13, fontWeight: 800, lineHeight: 1.2 }}>{l1}</div>
                    <div style={{ fontSize: 11, opacity: 0.75 }}>{l2}</div>
                  </div>
                ))}
              </div>
            </div>
            <div style={{ fontSize: 120, lineHeight: 1, filter: 'drop-shadow(0 8px 24px rgba(0,0,0,0.3))', animation: 'float 3s ease-in-out infinite' }}>🌿</div>
          </div>
        </div>
      </section>

      {/* Offer Banner */}
      <div style={{ background: 'var(--orange)', color: 'white', padding: '11px 0', textAlign: 'center' }}>
        <div style={{ fontFamily: 'var(--font-display)', fontSize: 15, fontWeight: 700 }}>
          🎉 Free Delivery on orders above ₹499 &nbsp;|&nbsp; 💬 Chat with us on WhatsApp: <strong>+91 98498 54853</strong>
        </div>
      </div>

      <div className="container">
        {/* Category Grid */}
        <section style={{ margin: '40px 0' }}>
          <div style={{ marginBottom: 24 }}>
            <h2 style={{ fontFamily: 'var(--font-display)', fontSize: 30, color: 'var(--gray-800)' }}>Shop by Category</h2>
            <p style={{ color: 'var(--gray-400)', fontSize: 16, marginTop: 4 }}>Everything fresh, delivered fast to your home</p>
          </div>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(148px, 1fr))', gap: 14 }}>
            {CATEGORIES.map(cat => (
              <Link key={cat.id} to={`/shop?category=${cat.id}`}
                style={{ background: cat.color, border: `2px solid ${cat.border}`, borderRadius: 18, padding: '22px 12px', textAlign: 'center', textDecoration: 'none', transition: 'transform 0.2s, box-shadow 0.2s', display: 'block' }}
                onMouseEnter={e => { e.currentTarget.style.transform = 'translateY(-5px)'; e.currentTarget.style.boxShadow = '0 10px 24px rgba(0,0,0,0.12)'; }}
                onMouseLeave={e => { e.currentTarget.style.transform = 'translateY(0)'; e.currentTarget.style.boxShadow = 'none'; }}>
                <div style={{ fontSize: 44, marginBottom: 10 }}>{cat.emoji}</div>
                <div style={{ fontFamily: 'var(--font-display)', fontWeight: 800, fontSize: 14, color: 'var(--gray-800)', marginBottom: 4 }}>{cat.label}</div>
                <div style={{ fontSize: 11, color: 'var(--gray-500)' }}>{cat.desc}</div>
              </Link>
            ))}
          </div>
        </section>

        {/* Featured Products */}
        <section style={{ margin: '40px 0' }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 20 }}>
            <div>
              <h2 style={{ fontFamily: 'var(--font-display)', fontSize: 28, color: 'var(--gray-800)' }}>⭐ Today's Top Picks</h2>
              <p style={{ color: 'var(--gray-400)', fontSize: 15, marginTop: 4 }}>Our freshest & most loved products</p>
            </div>
            <Link to="/shop" style={{ color: 'var(--green)', fontWeight: 800, fontSize: 16, fontFamily: 'var(--font-display)', textDecoration: 'none' }}>View All →</Link>
          </div>
          {loading ? (
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(190px, 1fr))', gap: 16 }}>
              {Array.from({ length: 8 }).map((_, i) => (
                <div key={i} style={{ background: 'white', borderRadius: 16, height: 330, overflow: 'hidden' }}>
                  <div className="skeleton" style={{ height: 160, borderRadius: 0 }} />
                  <div style={{ padding: 14, display: 'flex', flexDirection: 'column', gap: 8 }}>
                    <div className="skeleton" style={{ height: 12, width: '50%', borderRadius: 6 }} />
                    <div className="skeleton" style={{ height: 18, width: '75%', borderRadius: 6 }} />
                    <div className="skeleton" style={{ height: 28, borderRadius: 6 }} />
                    <div className="skeleton" style={{ height: 38, borderRadius: 8 }} />
                  </div>
                </div>
              ))}
            </div>
          ) : (
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(190px, 1fr))', gap: 16 }}>
              {featured.map(p => <ProductCard key={p._id} product={p} />)}
            </div>
          )}
        </section>

        {/* How it Works */}
        <section style={{ background: 'linear-gradient(135deg, var(--green-bg), var(--orange-bg))', borderRadius: 24, padding: '40px 32px', margin: '40px 0' }}>
          <h2 style={{ fontFamily: 'var(--font-display)', fontSize: 28, textAlign: 'center', marginBottom: 32, color: 'var(--green-dark)' }}>How It Works — Simple as 1-2-3 📱</h2>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: 24 }}>
            {[
              ['1', '📱', 'Login with Phone', 'Enter your mobile number — no password needed!'],
              ['2', '🛒', 'Add to Cart', 'Browse & add your favourite fresh products'],
              ['3', '🚚', 'Delivered Fresh', 'We deliver to your door, you pay cash — simple!'],
            ].map(([num, icon, title, desc]) => (
              <div key={num} style={{ textAlign: 'center', padding: '16px' }}>
                <div style={{ width: 60, height: 60, background: 'var(--green)', borderRadius: '50%', display: 'flex', alignItems: 'center', justifyContent: 'center', margin: '0 auto 12px', fontFamily: 'var(--font-display)', fontWeight: 900, fontSize: 24, color: 'white' }}>{num}</div>
                <div style={{ fontSize: 36, marginBottom: 10 }}>{icon}</div>
                <h3 style={{ fontFamily: 'var(--font-display)', fontSize: 18, color: 'var(--green-dark)', marginBottom: 6 }}>{title}</h3>
                <p style={{ fontSize: 14, color: 'var(--gray-600)', lineHeight: 1.6 }}>{desc}</p>
              </div>
            ))}
          </div>
        </section>

        {/* Why Choose Us */}
        <section style={{ margin: '40px 0' }}>
          <h2 style={{ fontFamily: 'var(--font-display)', fontSize: 28, textAlign: 'center', marginBottom: 28, color: 'var(--gray-800)' }}>Why Customers Love Desi2Door 💚</h2>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: 18 }}>
            {[
              ['🌿', 'Always Fresh', 'Sourced directly from local farms every single morning'],
              ['🚚', 'Fast Delivery', 'Same-day delivery right to your doorstep'],
              ['💰', 'Best Prices', 'No middlemen — direct farm to door pricing'],
              ['📱', 'Easy Ordering', 'Just your phone number, no passwords needed!'],
              ['🤝', 'Pure Quality', '100% natural, zero chemicals or preservatives'],
              ['💳', 'Cash on Delivery', 'Pay only when you receive your order!'],
            ].map(([icon, title, desc]) => (
              <div key={title} style={{ background: 'white', borderRadius: 18, padding: '24px 18px', textAlign: 'center', boxShadow: '0 2px 10px rgba(0,0,0,0.06)' }}>
                <div style={{ fontSize: 40, marginBottom: 10 }}>{icon}</div>
                <h3 style={{ fontFamily: 'var(--font-display)', fontSize: 16, color: 'var(--green-dark)', marginBottom: 6 }}>{title}</h3>
                <p style={{ fontSize: 13, color: 'var(--gray-500)', lineHeight: 1.5 }}>{desc}</p>
              </div>
            ))}
          </div>
        </section>
      </div>

      <style>{`@keyframes float { 0%,100% { transform: translateY(0) } 50% { transform: translateY(-12px) } }`}</style>
    </div>
  );
};

export default HomePage;
